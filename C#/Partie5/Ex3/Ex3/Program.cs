using System;

namespace Ex3
{
    class Program
    {
        static void Main(string[] args)
        {
            ColorDisplay colorDisplay = new ColorDisplay();
            //colorDisplay.hWorld();
            colorDisplay.hFlag();


            /*String words;
            String textColor;
            String backgroundColor;

            Console.WriteLine("Saisir votre phrase");
            words = Console.ReadLine();

            Console.WriteLine("Saisir couleur de texte : Red ou Yellow");
            textColor = Console.ReadLine();

            while (!textColor.Equals("Red") && !textColor.Equals("Yellow"))
            {
                Console.WriteLine("La couleur doit être Red ou Yellow");
            }

            Console.WriteLine("Saisir couleur de fond : Black ou White");
            backgroundColor = Console.ReadLine();

            while (!backgroundColor.Equals("Black") && !backgroundColor.Equals("White"))
            {
                Console.WriteLine("La couleur doit être Black ou White");
            }

            colorDisplay.worldColorFullDisplay(words, textColor, backgroundColor);
            */
        }
    }
}
